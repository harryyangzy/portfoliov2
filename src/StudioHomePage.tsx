import PortfolioShell from './PortfolioShell'

/** Studio landing: sidebar / mobile nav only (no project canvas). */
export default function StudioHomePage() {
  return <PortfolioShell pageName="Home">{null}</PortfolioShell>
}
